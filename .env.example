# Copy this file to .env and fill in the values DirectAdmin gives you
# when you create the MySQL database (Extra Features -> MySQL Management).

DB_HOST=localhost
DB_USER=teoihwmx_yourdbuser
DB_PASSWORD=your-db-password
DB_NAME=teoihwmx_slippers

# DirectAdmin's Node Selector sets PORT for you automatically — you can
# usually leave this line out entirely once deployed. It's here for local testing.
PORT=3000
